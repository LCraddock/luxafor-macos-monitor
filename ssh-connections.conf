# SSH Connection Configuration
# Format: NAME|HOST|PORT|USER|DESCRIPTION

# Elasticsearch server
elasticsearch-server|34.23.95.245|822|larry.craddock|Elasticsearch Server

# SSH config aliases (use "config" for port and user)
ip-scanner|ip-scanner|config|config|IP Scanner

# Add more SSH connections here
# Example:
# dev-server|dev.example.com|22|username|Development Server
# staging|staging.example.com|22|username|Staging Environment
# For SSH config aliases: alias-name|alias-name|config|config|Description